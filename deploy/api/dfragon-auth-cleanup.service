[Unit]
Description=DFRAGON expired authentication data cleanup
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
WorkingDirectory=/opt/dfragon/deploy/api
ExecStart=/usr/bin/docker compose --profile maintenance run --rm --no-deps cleanup
TimeoutStartSec=15min
