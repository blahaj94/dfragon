import { actionButton } from '@seed-design/css/recipes/action-button'

export const primary = actionButton({ variant: 'neutralSolid', size: 'medium' })
export const secondary = actionButton({ variant: 'neutralOutline', size: 'medium' })
